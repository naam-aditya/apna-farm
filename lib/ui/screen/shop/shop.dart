import 'package:apna_farm/ui/widgets/app_bar.dart';
import 'package:flutter/material.dart';

class ApnaShopScreen extends StatelessWidget {
  const ApnaShopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: ApnaAppBar(), body: Scaffold());
  }
}
